import static org.junit.Assert.*;
import org.junit.Test;

import javax.annotation.processing.SupportedSourceVersion;

/** Write a single JUnit test file with @Test.
 *  Randomly call methods from ArrayDequeSolution and
 *  StudentArrayDeque until we get a disagreement. */
public class TestArrayDequeGold {
    @Test
    public void testArrayDeque() {
        ArrayDequeSolution<Integer> expectedArrayDeque = new ArrayDequeSolution<>();
        StudentArrayDeque<Integer> studentArrayDeque = new StudentArrayDeque<>();

    }
}
